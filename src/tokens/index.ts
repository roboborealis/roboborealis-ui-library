/**
 * @roboborealis/components/tokens
 *
 * Design token constants for the RoboBorealis design system.
 * Import these when you need programmatic access to color values —
 * e.g. for chart palette configuration, dynamic styling, or tests.
 *
 * For CSS-driven theming in components, use the CSS variables from
 * `@roboborealis/components/theme-midnight` or `@roboborealis/components/theme-aurora` instead.
 *
 * @example
 * // Named constants — for one or two specific tokens
 * import { BRAND_BLUE, ACCENT_PRIMARY } from '@roboborealis/components/tokens';
 *
 * @example
 * // Grouped objects — for chart palettes or config objects
 * import { brandColors, semanticColors, accentColors } from '@roboborealis/components/tokens';
 * const chartPrimary = accentColors.primary; // '#BB5600'
 */

export * from './roboborealis-colors';
